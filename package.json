{
  "name": "astromik",
  "version": "0.0.1",
  "description": "monorepo for astromik",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "watch": "turbo watch",
    "release": "npm run build && changeset publish"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@changesets/cli": "^2.25.2",
    "turbo": "^1.6.3"
  }
}
